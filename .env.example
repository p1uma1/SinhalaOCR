# API server
APP_HOST=0.0.0.0
APP_PORT=8000
MAX_UPLOAD_MB=10

# Comma-separated allowed CORS origins for production
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Frontend (optional — leave empty when served from same origin)
# VITE_API_URL=
